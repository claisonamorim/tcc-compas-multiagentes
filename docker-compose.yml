services:
  tcc:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./:/app
      - ./outputs:/app/outputs
      - ./data:/app/data
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - LLM_MODEL=gpt-4.1-mini
